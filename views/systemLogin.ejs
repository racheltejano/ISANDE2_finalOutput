<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>System Login</title>
    <style>
        /* Main Container */
        .systemLogin-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            max-width: 900px; /* Adjust width as needed */
            margin: 2rem auto;
            background-color: #fffdca; /* Match the background color of registration */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 15px;
            gap: 2rem; /* Space between form and image */
        }

        /* Form Section */
        .systemLogin-form-container {
            flex: 1; /* Takes up 50% of the space */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .systemLogin-text-header {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 1.5rem;
            color: #ffae50;
            font-weight: bold;
        }

        .systemLogin-form {
            width: 100%;
            display: flex;
            flex-direction: column;
        }

        .systemLogin-input {
            width: 100%;
            padding: 0.9rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: 10px;
            font-size: 1rem;
            box-sizing: border-box;
            background-color: #f9f9f9;
            transition: 0.3s ease;
        }

        .systemLogin-input:focus {
            border-color: #ffae50;
            background-color: #fff;
            outline: none;
        }

        .systemLogin-button {
            width: 100%;
            padding: 1rem;
            background-color: #ffae50;
            color: #fff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: bold;
            transition: 0.3s ease;
        }

        .systemLogin-button:hover {
            background-color: #c27b28;
        }

        /* Image Section */
        .systemLogin-image-container {
            flex: 1; /* Takes up 50% of the space */
            text-align: center;
        }

        .systemLogin-image {
            width: 100%;
            max-width: 400px; /* Adjust the max width of the image */
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <!-- Main Login Section -->
    <div class="systemLogin-container">
        <!-- Form Section -->
        <div class="systemLogin-form-container">
            <h1 class="systemLogin-text-header">Seller Login</h1>
            <form class="systemLogin-form" method="POST" action="/systemLogin">
                <input class="systemLogin-input" type="text" placeholder="Email" name="username">
                <input class="systemLogin-input" type="password" placeholder="Password" name="password"> 
                <button class="systemLogin-button" type="submit">Log In</button> 
            </form>
        </div>

        <!-- Image Section -->
        <div class="systemLogin-image-container">
            <img src="/images/login-gif.gif" alt="System Login GIF" class="systemLogin-image">
        </div>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Merca-Finds Login</title>
    <style>
        /* Main Container */
        .websiteLogin-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            max-width: 900px; /* Adjust width as needed */
            margin: 2rem auto;
            background-color: #fffdca; /* Light background color */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 15px;
            gap: 2rem; /* Space between form and any additional elements */
        }

        /* Form Section */
        .websiteLogin-form-container {
            flex: 1; /* Takes up 50% of the space */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .websiteLogin-text-header {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 1.5rem;
            color: #ffae50; /* Warm accent color */
            font-weight: bold;
        }

        .websiteLogin-form {
            width: 100%;
            display: flex;
            flex-direction: column;
        }

        .websiteLogin-input {
            width: 100%;
            padding: 0.9rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: 10px;
            font-size: 1rem;
            box-sizing: border-box;
            background-color: #f9f9f9;
            transition: 0.3s ease;
        }

        .websiteLogin-input:focus {
            border-color: #ffae50;
            background-color: #fff;
            outline: none;
        }

        .websiteLogin-button {
            width: 100%;
            padding: 1rem;
            background-color: #ffae50;
            color: #fff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: bold;
            transition: 0.3s ease;
        }

        .websiteLogin-button:hover {
            background-color: #c27b28;
        }

        /* Optional Image Section */
        /* Add this section if you want an image (like the second example) */
        .websiteLogin-image-container {
            flex: 1; /* Takes up 50% of the space */
            text-align: center;
        }

        .websiteLogin-image {
            width: 100%;
            max-width: 400px; /* Adjust the max width of the image */
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <!-- Main Login Section -->
    <div class="websiteLogin-container">
        <!-- Form Section -->
        <div class="websiteLogin-form-container">
            <h1 class="websiteLogin-text-header">Customer Login</h1>
            <form class="websiteLogin-form" method="POST" action="/websiteLogin">
                <input class="websiteLogin-input" type="text" placeholder="Email" name="username">
                <input class="websiteLogin-input" type="password" placeholder="Password" name="password"> 
                <button class="websiteLogin-button" type="submit">Log In</button> 
            </form>
        </div>

        <!-- Optional Image Section (if needed) -->
        <div class="websiteLogin-image-container">
            <img src="/images/login-gif.gif" alt="Website Login GIF" class="websiteLogin-image">
        </div>
    </div>
</body>
</html>
